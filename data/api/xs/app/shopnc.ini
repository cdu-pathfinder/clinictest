project.name = clinicnc
server.index = 127.0.0.1:8383
server.search = 127.0.0.1:8384

[pk]
type = id

[doctors_id]
type = numeric

[doctors_name]
type = title
index = mixed
weight = 5

[gc_name]
index = mixed
weight = 3

[brand_name]
index = mixed
weight = 4

[cate_1]
index = self
tokenizer = full

[cate_2]
index = self
tokenizer = full

[cate_3]
index = self
tokenizer = full

[brand_id]
index = self
tokenizer = full

[attr_id]
index = self
tokenizer = split(_)

[clic_id]
index = self
tokenizer = full

[area_id]
index = self
tokenizer = full

[doctors_price]
type = numeric

[doctors_click]
type = numeric

[doctors_salenum]
type = numeric

[gc_id]